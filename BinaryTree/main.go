package main

import (
	"fmt"
	"reflect"
)

type BinaryTree struct {
	left *BinaryTree
	right *BinaryTree
	value interface{}
}

func NewBinaryTree(type) *BinaryTree {
	return 
} 

func (bt *BinaryTree) push(value interface{}) {
	s.data = append(s.data, value)
}




func main() {
	stack := BinaryTree{}
	stack.comparison = func(value int) {
		if stack.
	}
	stack.push(2)
	fmt.Println(stack.len())
	fmt.Println(stack.pop())
	fmt.Println(stack.pop())
	fmt.Println(stack.pop())
}